version: "3.8"

services:
  kometa:
    image: kometateam/kometa:latest
    container_name: kometa
    restart: unless-stopped
    environment:
      - TZ=${TZ:-America/New_York}
      - KOMETA_CONFIG=/config/config.yml
      - KOMETA_RUN=true
      - KOMETA_TIMES=${KOMETA_TIMES:-06:00}
    volumes:
      - ./kometa-config:/config
      - ../kometa/assets:/assets:ro
      - ${MEDIA_ROOT:-/data/media}:/data/media:ro
    env_file:
      - .env

# Uncomment to add Posterizarr
#  posterizarr:
#    image: ghcr.io/fscorrupt/posterizarr:latest
#    container_name: posterizarr
#    restart: unless-stopped
#    environment:
#      - TZ=${TZ:-America/New_York}
#    volumes:
#      - ./posterizarr-config:/config
#      - ../kometa/assets:/assets
#      - ${MEDIA_ROOT:-/data/media}:/data/media:ro
#    env_file:
#      - .env

networks:
  default:
    name: media-stack
